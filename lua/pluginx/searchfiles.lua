-- Instalamos Telescope -- plugin que nos ayuda a explorar entre archivos.
return {
  'nvim-telescope/telescope.nvim', tag = '0.1.6', -- o la última versión estable
  dependencies = { 'nvim-lua/plenary.nvim' }
}
